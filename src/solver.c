#include "../includes/filler.h"

void        get_moves(t_move *list, t_data *data)
{

}

void        solve(t_data *data)
{
    t_move  *list;

    list = 0;
    get_moves(&list, data);
    //order_moves(&list);
    //print_move(list);
    //free_list(&list);
}